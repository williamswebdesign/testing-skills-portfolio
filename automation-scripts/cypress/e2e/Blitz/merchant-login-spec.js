/**
 * this tests that the merchant login works
 * https://hub.blytzpay.com/#/login?next=%2F
 * check that when leaving email blank red Email: this field may not be blank
 * check that password can not be blank
 */

describe.skip('Checks the merchant login UI works', () => {
    it('can login as a merchant', () => {

    });

    it('can logout as a merchant', () => {

    });
});